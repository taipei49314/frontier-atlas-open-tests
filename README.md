# Frontier Atlas open test surface

Offline, publishable protocol and verifier candidate for Frontier Atlas public
testing. This repository does **not** contain the closed core, production
prompts/routing, private answer keys, raw annotations, gold labels, holdout
content, user documents, or secrets.

P3 will add canonical JSON, packet validation, commitment/reveal verification,
qualification submission validation, stable JSON output, and tamper tests. It
will remain local with no remote until an explicit publication review.

Research baseline provenance:
`C:\GitHub-Apps\frontier-atlas@0db3118`.

